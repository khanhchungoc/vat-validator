# Vietnamese Translation & Landscape PDF Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Translate all user interface elements, automation logs, status labels, and generated reports (PDF & Excel) into Vietnamese, and verify the PDF is exported in horizontal A4 layout.

**Architecture:** Update front-end React components with Vietnamese strings, adjust status code mappings, translate backend logs generated by the GDT automation engine, adjust the generated PDF's layout text, and update generated Excel headers and column styling.

**Tech Stack:** React, Node.js, Express, Playwright, xlsx-js-style.

---

### Task 1: React Component Translations (Frontend UI)

**Files:**
- Modify: `src/App.jsx`
- Modify: `src/components/CaptchaModal.jsx`
- Modify: `src/components/DownloadButtons.jsx`
- Modify: `src/components/DropZone.jsx`
- Modify: `src/components/DuplicateWarning.jsx`
- Modify: `src/components/ErrorBanner.jsx`
- Modify: `src/components/InvoiceQueue.jsx`
- Modify: `src/components/LiveConsole.jsx`
- Modify: `src/components/ManualEntryForm.jsx`
- Modify: `src/components/ModeToggle.jsx`
- Modify: `src/components/ProgressBar.jsx`
- Modify: `src/components/ResumePanel.jsx`
- Modify: `src/components/StepButton.jsx`

- [ ] **Step 1: Translate src/App.jsx UI text**
  Modify the user-facing text strings in `src/App.jsx`.
  Change:
  - Line 278: `{showConsole ? '✕ Close Log' : '📋 Activity Log'}` -> `{showConsole ? '✕ Đóng nhật ký' : '📋 Nhật ký hoạt động'}`
  - Line 294: `<strong style={{ color: 'var(--fail)', display: 'block', marginBottom: 6 }}>⚠️ Processing Error</strong>` -> `<strong style={{ color: 'var(--fail)', display: 'block', marginBottom: 6 }}>⚠️ Lỗi xử lý</strong>`
  - Line 317: `🚀 Start Processing` -> `🚀 Bắt đầu xử lý`
  - Line 324: `🔄 Reset Skipped to Pending` -> `🔄 Khôi phục trạng thái Chờ`
  - Line 336: `+ Add Invoice Manually` -> `＋ Thêm hóa đơn thủ công`
  - Line 344: `🧹 Clear & New Session` -> `🧹 Xóa & Phiên mới`
  - Line 351: `🛑 Stop` -> `🛑 Dừng`
  - Line 146: `'Failed to send manual invoice: WebSocket is disconnected.'` -> `'Không thể gửi hóa đơn thủ công: WebSocket mất kết nối.'`
  - Line 222: `'Failed to create session: ' + (data.error || 'Unknown error')` -> `'Không thể tạo phiên làm việc: ' + (data.error || 'Lỗi không xác định')`
  - Line 226: `'Failed to create session'` -> `'Không thể tạo phiên làm việc'`
  - Line 234: `'Failed to start processing: WebSocket is disconnected.'` -> `'Không thể bắt đầu xử lý: WebSocket mất kết nối.'`
  - Line 242: `'Failed to stop processing: WebSocket is disconnected.'` -> `'Không thể dừng xử lý: WebSocket mất kết nối.'`
  - Line 252: `'Failed to advance step: WebSocket is disconnected.'` -> `'Không thể tiếp tục bước tiếp theo: WebSocket mất kết nối.'`
  - Line 263: `'Failed to update mode: WebSocket is disconnected.'` -> `'Không thể cập nhật chế độ: WebSocket mất kết nối.'`
  - Line 190: `data.error || 'Failed to resume session'` -> `data.error || 'Không thể khôi phục phiên làm việc'`
  - Line 193: `'Failed to resume session'` -> `'Không thể khôi phục phiên làm việc'`

- [ ] **Step 2: Translate src/components/CaptchaModal.jsx**
  Modify `src/components/CaptchaModal.jsx`.
  Change:
  - Line 25: `🔑 CAPTCHA Solve Required` -> `🔑 Yêu cầu giải mã CAPTCHA`
  - Line 29: `Enter the CAPTCHA below, or click directly on the <strong>opened browser window</strong> to solve it on the site.` -> `Nhập mã CAPTCHA bên dưới hoặc giải trực tiếp trên <strong>cửa sổ trình duyệt đang mở</strong> để tự động tiếp tục.`
  - Line 34: `❌ Incorrect CAPTCHA entered. Please try GDT's refreshed code (Attempt {attempt})` -> `❌ Mã CAPTCHA không chính xác. Vui lòng nhập mã mới từ GDT (Lần thử {attempt})`
  - Line 60: `placeholder="Enter CAPTCHA..."` -> `placeholder="Nhập mã CAPTCHA..."`
  - Line 86: `Verify` -> `Xác nhận`
  - Line 93: `Or solve directly in the GDT browser window...` -> `Hoặc giải trực tiếp trên trình duyệt GDT...`
  - Line 103: `Skip Invoice` -> `Bỏ qua hóa đơn`

- [ ] **Step 3: Translate src/components/DownloadButtons.jsx**
  Modify `src/components/DownloadButtons.jsx`.
  Change:
  - Line 6: `<h3>✅ Batch Complete — Download Results</h3>` -> `<h3>✅ Hoàn thành loạt xử lý — Tải kết quả</h3>`
  - Line 10: `📄 Download PDF` -> `📄 Tải tệp PDF`
  - Line 15: `📊 Download Excel Summary` -> `📊 Tải tệp Excel tổng hợp`

- [ ] **Step 4: Translate src/components/DropZone.jsx**
  Modify `src/components/DropZone.jsx`.
  Change:
  - Line 70: `? <p>Uploading...</p>` -> `? <p>Đang tải lên...</p>`
  - Line 72: `? <p>🔒 Processing active...</p>` -> `? <p>🔒 Đang trong quá trình xử lý...</p>`
  - Line 73: `: <p>📂 Drop XML files here or click to browse</p>` -> `: <p>📂 Kéo thả các tệp XML vào đây hoặc click để duyệt tệp</p>`
  - Line 31: ``Failed to upload invoices: ${e.message}`` -> ``Không thể tải lên hóa đơn: ${e.message}``
  - Line 33: ``Failed to upload invoices: ${e.message}`` -> ``Không thể tải lên hóa đơn: ${e.message}``

- [ ] **Step 5: Translate src/components/DuplicateWarning.jsx**
  Modify `src/components/DuplicateWarning.jsx`.
  Change:
  - Line 7: `<h3>⚠️ Duplicate Invoices Detected</h3>` -> `<h3>⚠️ Phát hiện hóa đơn trùng lặp</h3>`
  - Line 9: `The following invoice IDs already exist in the queue. Remove them or proceed anyway.` -> `Các mã hóa đơn sau đã tồn tại trong hàng đợi. Vui lòng loại bỏ hoặc tiếp tục.`
  - Line 19: `Remove Duplicates` -> `Loại bỏ trùng lặp`
  - Line 20: `Proceed Anyway` -> `Vẫn tiếp tục`

- [ ] **Step 6: Translate src/components/ErrorBanner.jsx**
  Modify `src/components/ErrorBanner.jsx`.
  Change:
  - Line 9: `<strong>Processing Paused</strong>` -> `<strong>Đang tạm dừng xử lý</strong>`
  - Line 14: `Skip Invoice` -> `Bỏ qua hóa đơn`
  - Line 15: `Retry` -> `Thử lại`

- [ ] **Step 7: Translate src/components/InvoiceQueue.jsx**
  Modify `src/components/InvoiceQueue.jsx`.
  Change:
  - Line 2: `pending: { label: 'Pending', color: 'var(--text-muted)' }` -> `pending: { label: 'Đang chờ', color: 'var(--text-muted)' }`
  - Line 3: `processing: { label: 'Processing...', color: 'var(--accent)' }` -> `processing: { label: 'Đang xử lý...', color: 'var(--accent)' }`
  - Line 4: `pass: { label: '✅ Pass', color: 'var(--pass)' }` -> `pass: { label: '✅ Hợp lệ', color: 'var(--pass)' }`
  - Line 5: `invalid-invoice: { label: '❌ Invalid Invoice', color: 'var(--fail)' }` -> `'invalid-invoice': { label: '❌ Hóa đơn không hợp lệ', color: 'var(--fail)' }`
  - Line 6: `invalid-business: { label: '❌ Invalid Business', color: 'var(--fail)' }` -> `'invalid-business': { label: '❌ DN không hoạt động', color: 'var(--fail)' }`
  - Line 7: `skipped: { label: '⚠️ Skipped', color: 'var(--skip)' }` -> `skipped: { label: '⚠️ Đã bỏ qua', color: 'var(--skip)' }`
  - Line 15: `<h3>Invoice Queue ({invoices.length})</h3>` -> `<h3>Hàng đợi hóa đơn ({invoices.length})</h3>`

- [ ] **Step 8: Translate src/components/LiveConsole.jsx**
  Modify `src/components/LiveConsole.jsx`.
  Change:
  - Line 30: `<span className="console-title-text">&gt;_ Live Activity Log</span>` -> `<span className="console-title-text">&gt;_ Nhật ký hoạt động thời gian thực</span>`
  - Line 41: `<span>Waiting for automation to start. Real-time steps will appear here...</span>` -> `<span>Đang chờ tiến trình tự động bắt đầu. Các bước thực hiện sẽ hiển thị ở đây...</span>`

- [ ] **Step 9: Translate src/components/ManualEntryForm.jsx**
  Modify `src/components/ManualEntryForm.jsx`.
  Change:
  - Line 20: ``Required fields missing: ${missing.join(', ')}`` -> ``Thiếu các thông tin bắt buộc: ${missing.join(', ')}``
  - Line 26: `'Total Amount must be a valid positive number.'` -> `'Tổng tiền phải là số dương hợp lệ.'`
  - Line 40: `{ name: 'invoiceCode', label: 'Invoice Code (Ký hiệu)', required: true }` -> `{ name: 'invoiceCode', label: 'Ký hiệu hóa đơn', required: true }`
  - Line 41: `{ name: 'invoiceNumber', label: 'Invoice Number (Số HĐ)', required: true }` -> `{ name: 'invoiceNumber', label: 'Số hóa đơn', required: true }`
  - Line 42: `{ name: 'sellerName', label: 'Seller Name', required: true }` -> `{ name: 'sellerName', label: 'Tên người bán', required: true }`
  - Line 43: `{ name: 'taxId', label: 'Tax ID (MST)', required: true }` -> `{ name: 'taxId', label: 'Mã số thuế (MST)', required: true }`
  - Line 44: `{ name: 'sellerAddress', label: 'Seller Address', required: false }` -> `{ name: 'sellerAddress', label: 'Địa chỉ người bán', required: false }`
  - Line 45: `{ name: 'totalAmount', label: 'Total Amount (VND)', required: true, type: 'number' }` -> `{ name: 'totalAmount', label: 'Tổng tiền (VND)', required: true, type: 'number' }`
  - Line 51: `<h3>Add Invoice Manually</h3>` -> `<h3>Thêm hóa đơn thủ công</h3>`
  - Line 62: `<button type="button" className="btn-secondary" onClick={onClose}>Cancel</button>` -> `<button type="button" className="btn-secondary" onClick={onClose}>Hủy</button>`
  - Line 63: `<button type="submit" className="btn-primary">Add Invoice</button>` -> `<button type="submit" className="btn-primary">Thêm hóa đơn</button>`

- [ ] **Step 10: Translate src/components/ModeToggle.jsx**
  Modify `src/components/ModeToggle.jsx`.
  Change:
  - Line 4: `<span className="mode-label">Processing Mode:</span>` -> `<span className="mode-label">Chế độ xử lý:</span>`
  - Line 12: `▶ Auto` -> `▶ Tự động`
  - Line 20: `👣 Step` -> `👣 Từng bước`
  - Line 10: `title="Process all invoices continuously"` -> `title="Xử lý liên tục tất cả hóa đơn"`
  - Line 18: `title="Pause after each invoice for review"` -> `title="Tạm dừng sau mỗi hóa đơn để kiểm tra"`

- [ ] **Step 11: Translate src/components/ProgressBar.jsx**
  Modify `src/components/ProgressBar.jsx`.
  Change:
  - Line 15: `<span className="progress-label">{done} / {total} invoices processed</span>` -> `<span className="progress-label">Đã xử lý {done} / {total} hóa đơn</span>`
  - Line 22: `<span style={{ color: 'var(--pass)' }}>✅ {passed} Pass</span>` -> `<span style={{ color: 'var(--pass)' }}>✅ {passed} Hợp lệ</span>`
  - Line 23: `<span style={{ color: 'var(--fail)' }}>❌ {failed} Failed</span>` -> `<span style={{ color: 'var(--fail)' }}>❌ {failed} Thất bại</span>`
  - Line 24: `<span style={{ color: 'var(--skip)' }}>⚠️ {skipped} Skipped</span>` -> `<span style={{ color: 'var(--skip)' }}>⚠️ {skipped} Đã bỏ qua</span>`

- [ ] **Step 12: Translate src/components/ResumePanel.jsx**
  Modify `src/components/ResumePanel.jsx`.
  Change:
  - Line 20: ``Are you sure you want to permanently delete the session "${session.id.replace(/_/g, ' ')}"? This action cannot be undone.`` -> ``Bạn có chắc chắn muốn xóa vĩnh viễn phiên làm việc "${session.id.replace(/_/g, ' ')}"? Thao tác này không thể hoàn tác.``
  - Line 35: `data.error || 'Failed to delete session'` -> `data.error || 'Không thể xóa phiên làm việc'`
  - Line 38: `'Failed to delete session'` -> `'Không thể xóa phiên làm việc'`
  - Line 46: `<h3>📁 Resume Previous Sessions</h3>` -> `<h3>📁 Khôi phục phiên làm việc trước</h3>`
  - Line 52: `{session.progress.done}/{session.progress.total} invoices completed` -> `Đã hoàn thành {session.progress.done}/{session.progress.total} hóa đơn`
  - Line 61: `Delete` -> `Xóa`
  - Line 64: `Resume →` -> `Tiếp tục →`

- [ ] **Step 13: Translate src/components/StepButton.jsx**
  Modify `src/components/StepButton.jsx`.
  Change:
  - Line 6: `Review done — Step → Next Invoice` -> `Xác nhận xem xong — Tiếp tục → Hóa đơn kế tiếp`

- [ ] **Step 14: Commit**
  Run:
  ```bash
  git add src/App.jsx src/components/
  git commit -m "feat: translate React frontend components to Vietnamese"
  ```

---

### Task 2: Backend Automation Log Translations

**Files:**
- Modify: `backend/automation/automationEngine.js`
- Modify: `backend/automation/gdtInvoicePortal.js`
- Modify: `backend/automation/gdtTaxpayerPortal.js`

- [ ] **Step 1: Translate backend/automation/automationEngine.js**
  Modify the engine progress messages logged to the console log step.
  Change:
  - Line 169: `logStep('engine', 'Phase 1 complete. Starting Tax ID lookups on GDT Taxpayer Portal...')` -> `logStep('engine', 'Hoàn thành Pha 1. Bắt đầu tra cứu Mã số thuế trên Cổng thông tin NNT...')`

- [ ] **Step 2: Translate backend/automation/gdtInvoicePortal.js**
  Modify the GDT Invoice portal scraper logs to Vietnamese.
  Change:
  - Line 21: ``onLog(`Site load check failed. Reloading GDT Portal page (attempt ${attempt}/${maxAttempts})...`)`` -> ``onLog(`Kiểm tra tải trang thất bại. Đang tải lại Cổng thông tin HĐĐT (Lần thử ${attempt}/${maxAttempts})...`)``
  - Line 24: ``onLog(`Verifying GDT Portal loaded successfully...`)`` -> ``onLog(`Đang xác minh Cổng thông tin HĐĐT tải thành công...`)``
  - Line 29: `onLog('GDT Portal loaded successfully!')` -> `onLog('Cổng thông tin HĐĐT đã tải thành công!')`
  - Line 32: ``onLog(`Site load check failed on attempt ${attempt}: ${err.message}`)`` -> ``onLog(`Tải trang thất bại tại lần thử ${attempt}: ${err.message}`)``
  - Line 54: `onLog('Navigating to GDT Portal (https://hoadondientu.gdt.gov.vn/)...')` -> `onLog('Đang truy cập Cổng thông tin HĐĐT (https://hoadondientu.gdt.gov.vn/)...')`
  - Line 66: `onLog('Bypassing announcement popup...')` -> `onLog('Đang đóng thông báo popup...')`
  - Line 72: `onLog('Already on GDT Portal. Resetting form fields for new query...')` -> `onLog('Đã ở trên Cổng thông tin HĐĐT. Đang đặt lại các trường dữ liệu...')`
  - Line 103: ``onLog(`Filling invoice details: Seller Tax ID (${invoice.taxId}), Code, Number, Amount...`)`` -> ``onLog(`Đang điền thông tin hóa đơn: MST bán (${invoice.taxId}), Ký hiệu, Số HĐ, Tổng tiền...`)``
  - Line 142: ``onLog(`Capturing CAPTCHA image (attempt ${attempt})...`)`` -> ``onLog(`Đang chụp ảnh mã CAPTCHA (Lần thử ${attempt})...`)``
  - Line 181: `onLog('Please solve the CAPTCHA directly in GDT\'s opened browser window...')` -> `onLog('Vui lòng giải CAPTCHA trực tiếp trên trình duyệt GDT đang mở...')`
  - Line 223: ``onLog(`Typing CAPTCHA answer "${electronAnswer}" into GDT portal...`)`` -> ``onLog(`Đang nhập mã CAPTCHA "${electronAnswer}" vào ô xác thực...`)``
  - Line 250: `onLog('GDT returned HTTP 401 (Incorrect CAPTCHA). Refreshing and retrying...')` -> `onLog('Cổng thông tin báo lỗi CAPTCHA (401). Đang làm mới và thử lại...')`
  - Line 282: `onLog('GDT returned HTTP 200 (Invoice not found). Capturing error screenshot...')` -> `onLog('Cổng thông tin phản hồi: Không tìm thấy hóa đơn. Đang chụp ảnh màn hình lỗi...')`
  - Line 295: `onLog('GDT returned HTTP 200 (Invoice verified!). Capturing success screenshot...')` -> `onLog('Cổng thông tin phản hồi: Hóa đơn hợp lệ! Đang chụp ảnh màn hình kết quả...')`
  - Line 314: `onLog('API response timed out or failed. Falling back to DOM verification checks...')` -> `onLog('Hết thời gian phản hồi API hoặc lỗi. Đang chuyển sang xác minh trên giao diện DOM...')`

- [ ] **Step 3: Translate backend/automation/gdtTaxpayerPortal.js**
  Modify the GDT Taxpayer portal scraper logs to Vietnamese.
  Change:
  - Line 27: `onLog('Navigating to Taxpayer Portal (https://tracuunnt.gdt.gov.vn/)...')` -> `onLog('Đang truy cập Cổng thông tin người nộp thuế (https://tracuunnt.gdt.gov.vn/)...')`
  - Line 30: `onLog('Already on Taxpayer Portal. Resetting form fields for new query...')` -> `onLog('Đã ở trên Cổng thông tin NNT. Đang đặt lại các trường dữ liệu...')`
  - Line 51: ``onLog(`Capturing CAPTCHA image (attempt ${attempt})...`)`` -> ``onLog(`Đang chụp ảnh mã CAPTCHA (Lần thử ${attempt})...`)``
  - Line 69: `onLog('Please solve the CAPTCHA directly in GDT\'s opened browser window...')` -> `onLog('Vui lòng giải CAPTCHA trực tiếp trên trình duyệt GDT đang mở...')`
  - Line 110: ``onLog(`Typing CAPTCHA answer "${electronAnswer}" into GDT portal...`)`` -> ``onLog(`Đang nhập mã CAPTCHA "${electronAnswer}" vào ô xác thực...`)``
  - Line 173: `onLog('GDT returned "Vui lòng nhập đúng mã xác nhận!" (Incorrect CAPTCHA). Refreshing and retrying...')` -> `onLog('Cổng thông tin báo sai mã xác nhận. Đang làm mới và thử lại...')`
  - Line 182: `onLog('Verification successful! Capturing business status screenshot...')` -> `onLog('Xác thực thành công! Đang chụp ảnh trạng thái hoạt động...')`
  - Line 191: `onLog('Taxpayer not found (Invalid business!). Capturing error screenshot...')` -> `onLog('Không tìm thấy thông tin người nộp thuế (Doanh nghiệp không hợp lệ/không hoạt động). Đang chụp ảnh lỗi...')`
  - Line 200: `onLog('CAPTCHA incorrect or timeout occurred. Refreshing and retrying...')` -> `onLog('Sai mã CAPTCHA hoặc hết thời gian chờ. Đang làm mới và thử lại...')`

- [ ] **Step 4: Commit**
  Run:
  ```bash
  git add backend/automation/
  git commit -m "feat: translate backend automation logs to Vietnamese"
  ```

---

### Task 3: Translate Generated Export Documents & Style A4 Landscape PDF

**Files:**
- Modify: `backend/output/pdfGenerator.js`
- Modify: `backend/output/xlsxGenerator.js`

- [ ] **Step 1: Translate generated PDF output and confirm horizontal page layout**
  Modify `backend/output/pdfGenerator.js`.
  Change:
  - Line 40: `<h2>Invoice: ${invoice.invoiceCode} / ${invoice.invoiceNumber}</h2>` -> `<h2>Hóa đơn: ${invoice.invoiceCode} / ${invoice.invoiceNumber}</h2>`
  - Line 41: `${invoice.status.replace('-', ' ').toUpperCase()}` -> translate the badge text properly using a mapping:
    ```javascript
    const getStatusLabel = (status) => {
      const mapping = {
        'pass': 'HỢP LỆ',
        'invalid-invoice': 'HÓA ĐƠN KHÔNG HỢP LỆ',
        'invalid-business': 'DN KHÔNG HOẠT ĐỘNG',
        'skipped': 'ĐÃ BỎ QUA',
        'pending': 'ĐANG CHỜ',
        'processing': 'ĐANG XỬ LÝ'
      }
      return mapping[status] || status.toUpperCase()
    }
    ```
    Then use `getStatusLabel(invoice.status)` in place of `invoice.status.replace('-', ' ').toUpperCase()`.
  - Line 44: `<tr><td>Seller</td><td>${invoice.sellerName}</td></tr>` -> `<tr><td>Người bán</td><td>${invoice.sellerName}</td></tr>`
  - Line 45: `<tr><td>Tax ID</td><td>${invoice.taxId}</td></tr>` -> `<tr><td>Mã số thuế</td><td>${invoice.taxId}</td></tr>`
  - Line 46: `<tr><td>Address</td><td>${invoice.sellerAddress || '—'}</td></tr>` -> `<tr><td>Địa chỉ</td><td>${invoice.sellerAddress || '—'}</td></tr>`
  - Line 47: `<tr><td>Total Amount</td><td>${formatAmount(invoice.totalAmount)}</td></tr>` -> `<tr><td>Tổng tiền</td><td>${formatAmount(invoice.totalAmount)}</td></tr>`
  - Line 50: `'GDT Invoice Portal — hoadondientu.gdt.gov.vn'` -> `'Cổng thông tin HĐĐT TCT — hoadondientu.gdt.gov.vn'`
  - Line 51: `'GDT Taxpayer Portal — tracuunnt.gdt.gov.vn'` -> `'Cổng thông tin NNT TCT — tracuunnt.gdt.gov.vn'`
  - Line 35: `'Not captured'` -> `'Không chụp được'`
  - Ensure the style font-family has fallbacks for Vietnamese Unicode characters:
    Line 59: `body { font-family: Arial, sans-serif; font-size: 11px; color: #222; }` (Arial already supports Vietnamese character sets perfectly).
  - Verify A4 landscape is defined:
    Line 58: `@page { size: A4 landscape; margin: 12mm; }`
    Line 99: `await page.pdf({ path: outputPath, format: 'A4', landscape: true, printBackground: true })`
    No changes are needed for the page layout parameters since they are already set to A4 landscape, but double check they are intact.

- [ ] **Step 2: Translate Excel export and adjust column widths**
  Modify `backend/output/xlsxGenerator.js`.
  Change:
  - Line 5-11:
    ```javascript
    const STATUS_LABELS = {
      pass: 'Pass',
      'invalid-invoice': 'Invalid Invoice',
      'invalid-business': 'Invalid Business',
      skipped: 'Skipped',
      pending: 'Pending'
    }
    ```
    Change to:
    ```javascript
    const STATUS_LABELS = {
      pass: 'Hợp lệ',
      'invalid-invoice': 'Hóa đơn không hợp lệ',
      'invalid-business': 'Doanh nghiệp không hoạt động',
      skipped: 'Đã bỏ qua',
      pending: 'Đang chờ'
    }
    ```
  - Line 29:
    ```javascript
    const header = ['#', 'Invoice Code', 'Invoice Number', 'Seller Name', 'Tax ID', 'Amount (VND)', 'Status']
    ```
    Change to:
    ```javascript
    const header = ['STT', 'Ký hiệu hóa đơn', 'Số hóa đơn', 'Tên người bán', 'Mã số thuế', 'Tổng tiền (VND)', 'Trạng thái']
    ```
  - Adjust column widths on Line 44:
    ```javascript
    ws['!cols'] = [ { wch: 4 }, { wch: 14 }, { wch: 16 }, { wch: 45 }, { wch: 18 }, { wch: 16 }, { wch: 20 } ]
    ```
    Change to:
    ```javascript
    ws['!cols'] = [ { wch: 5 }, { wch: 18 }, { wch: 18 }, { wch: 50 }, { wch: 18 }, { wch: 18 }, { wch: 25 } ]
    ```

- [ ] **Step 3: Commit**
  Run:
  ```bash
  git add backend/output/
  git commit -m "feat: translate PDF and Excel reports to Vietnamese, customize column widths"
  ```

---

### Task 4: Fix / Update Unit Tests to Match Translations

**Files:**
- Modify: `backend/__tests__/routes_download.test.js`
- Modify: `backend/__tests__/automationEngine_sessions.test.js`

- [ ] **Step 1: Check existing test status**
  Run: `npm run test` or check tests inside backend folder.
  Identify if any assertion fails due to translated strings.

- [ ] **Step 2: Update backend/__tests__/routes_download.test.js**
  Update the status/header checks if any expect English headers/labels.
  - Line 49: `expect(res.json).toHaveBeenCalledWith({ error: 'PDF not found' })`
    Change: Let's check `backend/routes/download.js`.
    Ah! The routes themselves:
    `res.status(404).json({ error: 'PDF not found' })` -> Wait, should we translate the JSON API errors? No, typical REST API responses are in English and handled by UI code. The UI code was translated already. So API error bodies can stay as they are, but check if they match.
    Let's see if the test fails. If it does, update accordingly.

- [ ] **Step 3: Run the test command and verify all tests pass**
  Run: `npm run test` in `C:\Users\KhanhChuNgoc\Documents\Personal Projects\VATOCR` or run jest.
  Expected output: `PASS` on all test suites.

- [ ] **Step 4: Commit**
  Run:
  ```bash
  git add backend/__tests__/
  git commit -m "test: update unit tests to match translated string assets"
  ```
